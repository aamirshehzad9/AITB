# AITB Blue/Green Environment Mapping
# This file controls which color (blue/green) receives production traffic
# Updated automatically by the promotion pipeline

# Current active environment: BLUE
map $uri $webapp_env {
    default webapp_blue;
}

map $uri $inference_env {
    default inference_blue;
}

map $uri $dashboard_env {
    default dashboard_blue;
}

# Environment metadata
# Last updated: AUTO_GENERATED
# Active environment: blue
# Deployment version: DEPLOYMENT_VERSION
# Switch timestamp: SWITCH_TIMESTAMP